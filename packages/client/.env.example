# API Configuration
VITE_API_URL=http://localhost:3001/api

# For testing with Docker:
# VITE_API_URL=http://localhost:3080/api

# Production (VPS): set in server .env, client is served by the same host
